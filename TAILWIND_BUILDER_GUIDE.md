# دليل منشئ القوالب المخصصة - Tailwind Template Builder

## نظرة عامة

تم تصميم منشئ القوالب ليساعدك على إنشاء قوالب عروض وظيفية احترافية باستخدام تصاميم Figma والنصوص الديناميكية.

## سير العمل (6 خطوات)

### الخطوة 1: استيراد خلفية التصميم 📤

1. افتح تصميمك في Figma
2. **احذف جميع النصوص** من التصميم (أبقِ فقط الخلفيات والأشكال والألوان)
3. صدّر التصميم كصورة PNG بأبعاد **595×842 بكسل** (حجم A4)
4. حدد عدد الصفحات في القالب (1-10)
5. ارفع صورة الخلفية لكل صفحة

**💡 نصيحة:** يمكنك في Figma إخفاء طبقة النصوص بالكامل قبل التصدير للحصول على خلفية نظيفة.

---

### الخطوة 2: إدخال أنماط النصوص 📋

#### كيفية الحصول على الأنماط من Figma:

1. افتح Figma وحدد أي عنصر نصي
2. في لوحة الخصائص على اليمين، انقر **Inspect**
3. انسخ جميع خصائص النص:
   - `text-{color}` (اللون)
   - `text-{size}` (الحجم)
   - `font-{weight}` (الوزن)
   - `font-['Thmanyah_...']` (العائلة)
   - `leading-{height}` (ارتفاع السطر)
   - `tracking-{spacing}` (التباعد)

#### تنسيق الإدخال:

```
// اسم النمط أو مثال النص
text-black
text-sm
font-normal
font-['Thmanyah_sans_1.2']
leading-5
tracking-tight
---
// نمط آخر
text-gray-700
text-xs
font-bold
font-['Thmanyah_serif_display_1.2']
leading-5
tracking-wide
---
```

**⚠️ مهم:**
- السطر الأول لكل نمط يبدأ بـ `//` ويحتوي على اسم النمط أو مثال عليه
- افصل كل نمط عن الآخر بـ `---`
- كل خاصية في سطر منفصل

#### مثال من أنماطك:

```
// نتمنى أن تكون معنا في سعينا...
text-black
text-sm
font-normal
font-['Thmanyah_sans_1.2']
leading-5
tracking-tight
---
// المسمّى الوظيفي
text-black
text-sm
font-normal
font-['Thmanyah_sans_1.2']
leading-5
tracking-wide
---
// مدير محتوى
text-black
text-sm
font-bold
font-['Thmanyah_sans_1.2']
leading-5
tracking-wide
---
```

---

### الخطوة 3: إدخال أنماط الألوان 🎨

#### كيفية الحصول على الألوان من Figma:

1. في Figma، حدد أي عنصر ملون
2. في لوحة الخصائص، انقر على المربع الملون
3. انسخ قيمة **HEX** (مثل: `03BB6E`)

#### تنسيق الإدخال:

```
000000; // أسود
03BB6E; // أخضر ثمانية
315545; // أخضر داكن
3BC17B; // أخضر فاتح
ABD9AB; // أخضر باهت
F2EEE4; // بيج
```

**📌 ملاحظة:** كل لون في سطر منفصل بصيغة `HEX; // اسم اللون`

---

### الخطوة 4: إضافة عناصر النص 📝

الآن يمكنك إضافة النصوص على الخلفية بشكل مرئي:

1. **اختر الصفحة** من القائمة المنسدلة
2. انقر **إضافة نص** لإنشاء عنصر نصي جديد
3. سيظهر العنصر على الخلفية ويمكنك:
   - **سحبه وإفلاته** لتحديد الموضع
   - تحرير **محتوى النص**
   - اختيار **النمط** من الأنماط التي أدخلتها
   - ضبط الموضع بدقة عبر إدخال **X** و **Y** يدوياً

#### مثال:

- **النص:** "أهلاً أيمن"
- **النمط:** أهـلاً أيمن (text-3xl, font-black)
- **الموضع:** X: 261, Y: 238

**💡 نصيحة:** استخدم تصميم Figma كمرجع للمواضع الدقيقة.

---

### الخطوة 5: ربط الحقول بالبيانات 🔗

الآن اربط كل عنصر نص بحقل من بيانات النموذج:

#### الحقول المتاحة:

- **الاسم** - `formData.name`
- **المسمى الوظيفي** - `formData.jobTitle`
- **القسم** - `formData.department`
- **الفريق** - `formData.team`
- **المدير المباشر** - `formData.directManager`
- **نوع الدوام** - `formData.workType`
- **مدينة التعاقد** - `formData.contractCity`
- **المستوى** - `formData.level`
- **الراتب الشهري** - `formData.monthlySalary`
- **الراتب الأساسي** - `formData.basicSalary`
- **بدل السكن** - `formData.housingAllowance`
- **بدل النقل** - `formData.transportAllowance`

#### أمثلة الربط:

| النص في التصميم | الحقل المربوط |
|-----------------|---------------|
| "أيمن الحارثي" | الاسم |
| "مدير محتوى" | المسمى الوظيفي |
| "التطوير التنظيمي" | الفريق |
| "ثقافة المنظومة" | القسم |
| "كامل" | نوع الدوام |

**⚠️ ملاحظة:** النصوص الثابتة (مثل "المسمّى الوظيفي:", "الفريق:") اتركها بدون ربط.

---

### الخطوة 6: الحفظ والتصدير 💾

1. أدخل **اسم القالب** (مثل: "قالب ثمانية - الأزرق")
2. راجع **ملخص القالب**:
   - عدد الصفحات
   - عدد الأنماط
   - عدد النصوص
   - عدد الحقول المربوطة
3. انقر **حفظ وتصدير الكود**

سيتم:
- ✅ حفظ القالب في localStorage
- ✅ توليد كود TypeScript كامل
- ✅ تحميل ملف `.tsx` تلقائياً

---

## الكود المُولّد

### مثال على الكود الناتج:

```typescript
"use client"

import React from "react"
import { type JobOfferFormData } from "../Form/JobOfferFormTypes"

const styles = {
  style0: "text-black text-sm font-normal font-8-sans leading-5 tracking-tight",
  style1: "text-black text-sm font-bold font-8-sans leading-5 tracking-wide",
  style2: "text-black text-3xl font-black font-8-display tracking-wide",
}

interface قالبثمانيةProps {
  formData: JobOfferFormData
  editMode?: boolean
}

export default function قالبثمانية({ formData, editMode = false }: قالبثمانيةProps) {
  return (
    <div className="template-container">
      {/* صفحة 1 */}
      <div className="page-v2 relative" style={{ backgroundImage: 'url(data:image/png;base64,...)', backgroundSize: 'cover' }}>
        <div className={styles.style2} style={{ position: 'absolute', left: '261px', top: '238px' }}>
          {formData.name}
        </div>
        <div className={styles.style1} style={{ position: 'absolute', left: '261px', top: '300px' }}>
          {formData.jobTitle}
        </div>
        <div className={styles.style0} style={{ position: 'absolute', left: '50px', top: '400px' }}>
          "المسمى الوظيفي:"
        </div>
      </div>

    </div>
  )
}
```

### ما يحدث في الكود:

1. **تحويل الخطوط تلقائياً:**
   - `font-['Thmanyah_sans_1.2']` → `font-8-sans`
   - `font-['Thmanyah_serif_display_1.2']` → `font-8-display`
   - `font-['Thmanyah_serif_1.2']` → `font-8-serif`

2. **الخلفية مضمّنة بصيغة Base64**
   - لا حاجة لرفع الصور على السيرفر
   - تعمل مباشرة بعد النسخ

3. **الأنماط محفوظة في كائن styles**
   - سهل التعديل
   - لا تكرار

4. **النصوص الديناميكية**
   - `{formData.name}` → قيمة من البيانات
   - `"نص ثابت"` → يبقى كما هو

---

## استخدام القالب المُولّد

1. افتح الملف `.tsx` المحمّل
2. انسخ الكود
3. أنشئ ملف جديد في:
   ```
   src/components/JobOffer/Preview/قالبثمانية.tsx
   ```
4. الصق الكود
5. استورد القالب في الصفحة الرئيسية:
   ```typescript
   import قالبثمانية from "@/components/JobOffer/Preview/قالبثمانية"

   // في صفحة المعاينة:
   <قالبثمانية formData={formData} />
   ```

---

## طباعة PDF

القالب جاهز للطباعة بصيغة PDF:

1. افتح المعاينة في المتصفح
2. اضغط **Ctrl+P** (أو Cmd+P على Mac)
3. اختر:
   - **Destination:** Save as PDF
   - **Paper Size:** A4
   - **Margins:** None
   - **Scale:** 100%
4. احفظ الملف

**🎨 ملاحظة:** CSS المخصص في `globals.css` سيضبط الأبعاد تلقائياً للطباعة:
```css
@media print {
  .page-v2 {
    width: 210mm;
    height: 297mm;
    transform: scale(1.334);
  }
}
```

---

## نصائح مهمة

### ✅ أفضل الممارسات:

1. **استخدم أبعاد A4 الصحيحة:**
   - 595×842 بكسل (عرض × ارتفاع)
   - 210×297 ملم

2. **اجعل الخلفية نظيفة:**
   - بدون نصوص
   - فقط الأشكال والألوان

3. **أنماط واضحة:**
   - اجعل اسم كل نمط مفهوماً
   - مثال: "عنوان رئيسي"، "نص عادي"، "تسمية صغيرة"

4. **حدد المواضع بدقة:**
   - استخدم Figma كمرجع للمواضع
   - اضبط X و Y بدقة بكسل واحد

5. **اربط الحقول بذكاء:**
   - النصوص الثابتة (مثل "الاسم:") → لا تربط
   - القيم الديناميكية (مثل "أيمن") → اربط بحقل

### ⚠️ أخطاء شائعة:

1. ❌ **رفع خلفية بها نصوص**
   - ✅ احذف النصوص قبل التصدير

2. ❌ **نسيان الفاصل `---` بين الأنماط**
   - ✅ تأكد من وضع `---` بعد كل نمط

3. ❌ **إدخال ألوان بدون فاصلة منقوطة**
   - ❌ `03BB6E // أخضر`
   - ✅ `03BB6E; // أخضر`

4. ❌ **ربط نص ثابت بحقل**
   - النصوص مثل "الاسم:" يجب أن تبقى ثابتة

---

## دعم فني

إذا واجهت مشكلة:

1. **تحقق من وحدة التحكم (Console)** في المتصفح (F12)
2. تأكد من تنسيق الأنماط والألوان
3. راجع الكود المُولّد للتأكد من صحته

---

## الخلاصة

هذا المنشئ يوفر عليك **ساعات من البرمجة اليدوية**. الآن يمكنك:

- 🎨 تصميم في Figma بحرية
- 📋 نسخ الأنماط مباشرة
- 🖱️ ترتيب النصوص بصرياً
- 🔗 ربط الحقول ببساطة
- 💾 توليد كود TypeScript كامل
- 🖨️ طباعة PDF بجودة عالية

**وقت الإنشاء:** 10-15 دقيقة لكل قالب ✨
